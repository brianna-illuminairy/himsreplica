# Legacy / archived — do NOT use

These are the original React+Babel-in-browser prototype, design canvas, variant
explorations, and HTML mockups. **None of this renders the live funnel.**

The production funnel is the Next.js app in [`../app-next/`](../app-next/).
This folder is kept only for design reference and git history. Don't edit or
import from here.
